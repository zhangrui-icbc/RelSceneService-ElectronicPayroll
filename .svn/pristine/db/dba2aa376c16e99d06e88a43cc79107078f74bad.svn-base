package com.icbc.rel.hefei.service.todo.reimbursement.service;


import java.util.List;

import com.icbc.rel.hefei.entity.todo.reimbursement.ReCommonTemplate;

public interface ReCommonService {
	/**
	 * 定义共用模板字段
	 * @param reTemplateList
	 */
	List<ReCommonTemplate> insertReCommonTemplate(List<ReCommonTemplate> reTemplateList);
   /**
    * 删除共用模板字段
    * @param id
    */
   	void delCommonTemplate(int id);
   	/**
   	 * 修改共用模板字段
   	 * @param reCommonTemplate
   	 */
   	ReCommonTemplate updateCommonTemplate(ReCommonTemplate reCommonTemplate);
   	/**
   	 * 获取共用模板字段
   	 * @return
   	 */
	List<ReCommonTemplate> getCommonTemplate();
	/**
	 * 增减完固定字段后,调整自定义字段列号
	 * @param i
	 */
	void updateColIndex(int i);

}
